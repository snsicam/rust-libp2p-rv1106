# 2026-07-27

- 用户要在腾讯云服务器给 relay-server 开放 P2P 端口。确认：relay 用默认 4001 同时监听 TCP(中继电路) + UDP/QUIC(移动端直连+DCUtR 打洞)，两者都须开放。已给出腾讯云安全组/防火墙配置步骤，并强调必须设 `public_ip`(CVM 内网 OS 看到私网 IP，公网经 NAT)。无代码改动。已同步写入 MEMORY.md「Relay Server 部署」节。
